# skills

A collection of Claude Code skills.

## travel-brief

Generates a concise, actionable travel brief for any destination — transit cards, SIM/eSIM, ATMs, free city views, rooftop bars, food, currency, and entry requirements.

### Install

```bash
cp -r travel-brief ~/.claude/skills/
```

Then restart Claude Code (or open `/skills` to reload).
